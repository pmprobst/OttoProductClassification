# Archive

Standalone R scripts from before the pipeline refactor. Kept for reference. The active codebase is in the project root and `R/`; use `run_pipeline.R` or `run_single_models.R` to run the project.
